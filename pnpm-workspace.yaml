# Single-package repo. pnpm 9 requires `packages` whenever this file exists
# (approve-builds wrote allowBuilds without it, which breaks `pnpm store path` in CI).
packages:
  - "."

allowBuilds:
  better-sqlite3: true
  esbuild: true
